{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "engram": {
      // TODO: workaround temporal — {env:USERPROFILE_FWD} es USERPROFILE con forward slashes.
      // Bug: opencode expande {env:} antes del parse JSON, causando escapes invalidos (\U, \Y).
      // Revertir a {env:USERPROFILE}\\bin\\engram.exe cuando sst/opencode PR #14987 sea released.
      "command": [
        "{env:USERPROFILE_FWD}/bin/engram.exe",
        "mcp",
        "--tools=agent"
      ],
      "enabled": true,
      "type": "local"
    },
    "notion": {
      "type": "remote",
      "url": "https://mcp.notion.com/mcp",
      "enabled": true
    },
    "playwright": {
      "type": "local",
      "command": [
        "npx",
        "@playwright/mcp@latest"
      ],
      "enabled": true
    },
    "postgres": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@modelcontextprotocol/server-postgres",
        "{env:OPENCODE_POSTGRES_URL}"
      ],
      "enabled": true
    },
    "context7": {
      "type": "remote",
      "url": "https://mcp.context7.com/mcp",
      "enabled": true
    },
    "git": {
      "type": "local",
      "command": [
        "bun",
        "x",
        "@cyanheads/git-mcp-server@latest"
      ],
      // TODO: workaround temporal — se elimino el bloque environment con {env:PATH} por el mismo
      // bug de expansion de backslashes. Evaluar si es necesario restaurarlo cuando PR #14987 sea released.
      "enabled": true
    }
  }
}
